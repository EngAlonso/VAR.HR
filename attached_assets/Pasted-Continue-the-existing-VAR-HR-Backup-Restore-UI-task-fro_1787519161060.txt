Continue the existing VAR HR Backup & Restore UI task from the current imported implementation.

This project was migrated from another ChatGPT/Replit account. Do NOT restart the investigation or redo completed work.

## Current task

Separate the Backup and Restore-from-Device interfaces for BOTH:

- Platform Owner
- Company Owner

## What has already been confirmed

The backend already has separate operations and authorization paths for:

- Generated backups
- Downloading backups
- Uploading a backup from the device
- Restoring a backup

No backend redesign is required.

The previous agent inspected the BackupRestore frontend component and confirmed:

- Generated backups and uploaded restore records use the same backup record shape.
- Uploaded records can be identified using the existing metadata:
  metadata.sourceChecksum
- Generated backups do not have that uploaded-file metadata.
- The previous combined frontend patch FAILED to apply.
- NO source changes were made by that failed patch.

Therefore, continue from the existing code exactly as it is.

## Required UI

Create two clearly separated sections.

### 1. Backup / Download

This section is ONLY for backups generated by VAR HR.

Include the existing functionality for:

- Create Backup
- List generated backups
- Download backup

Do NOT show device-uploaded restore files in this section.

### 2. Restore from Device

This section is ONLY for restoring a backup file selected from the user's device.

Include:

- Select backup file
- Display selected filename
- Existing validation
- Existing restore action
- Existing restore confirmation/safety behavior

Make it visually obvious that this is a file coming FROM the device and is being used as input for Restore.

Do NOT mix restore/upload files with generated/downloadable backups.

## Scope

Apply the same UI separation to:

- Platform Owner
- Company Owner

Preserve existing authorization and scope rules.

Company Owner:
- Only access backups belonging to their own company.

Platform Owner:
- Preserve existing platform-level scope and permissions.

## Do NOT change

Do not modify:

- Backup format
- Database backup structure
- Existing validation
- Integrity checks
- Restore logic
- Tenant isolation
- Authorization
- Existing API contracts
- Automated employee-account flow
- Authentication

Do not add a new backup system.

Use the existing APIs and state/handlers.

## Localization

All new visible text must use the existing language system.

Arabic examples:

- النسخ الاحتياطية
- إنشاء نسخة احتياطية
- تنزيل النسخة الاحتياطية
- الاستعادة من الجهاز
- اختر ملف النسخة الاحتياطية
- الملف المحدد
- التحقق من النسخة
- استعادة النسخة

English equivalents must also exist.

Preserve RTL/LTR.

## Implementation approach

Use small, focused patches because the previous large patch failed to apply to the large BackupRestore component.

Before editing:
1. Confirm the working tree is unchanged from the imported state.
2. Inspect the existing BackupRestore component and handlers.
3. Reuse the existing generated-vs-uploaded distinction using metadata.sourceChecksum where appropriate.

Then implement only the UI separation.

## Verification

After implementation run:

- Full typecheck
- Frontend build
- API build only if affected
- Formatting/diff check
- Restart affected workflow
- Preview/runtime verification

Verify:
- Generated backups appear only in Backup/Download.
- Device-selected files appear only in Restore from Device.
- Download still works.
- Create Backup still works.
- File selection still works.
- Validation still works.
- Restore still works.
- Company Owner remains tenant-isolated.
- Platform Owner behavior remains unchanged.
- Arabic/English switch correctly.
- RTL/LTR remains correct.

Report exact files changed and whether any backend changes were required.

IMPORTANT:
Do not start over. Continue from the existing imported implementation and the facts above.