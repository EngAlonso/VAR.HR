Continue VAR HR Part 4 from the EXACT current repository state.

IMPORTANT:
Do NOT restart Part 4 from zero.
Do NOT redo Parts 1–3.
Do NOT start Part 5.
Read the existing "hand-off/" folder first and preserve all previous reports.

What is already completed

The previous Part 4 work has already:

- Audited the existing biometric-device foundation.
- Preserved the provider-neutral biometric architecture.
- Preserved the rule that no biometric vendor or fake hardware integration must be invented.
- Added/continued the existing Part 4 localization strings.
- Added localized UI text for:
  - biometric devices
  - device metadata
  - employee/device mappings
  - geofences
  - GPS states and errors
- Wired the Devices screen to the existing generated API hooks.
- Wired company-owned device CRUD.
- Added/connected device metadata.
- Added/connected employee ↔ device mappings.
- Added/connected company locations/geofence data.
- Preserved the existing localization system and RTL behavior.
- API typecheck: PASS.
- Frontend typecheck: PASS.
- Shared library typecheck: PASS.
- Existing Drizzle development schema was applied non-destructively.
- API workflow was restarted and is currently the next thing that must be verified.

The previous agent stopped immediately after restarting:

"artifacts/api-server: API Server"

So the current state is Part 4 in progress, not complete.

What is still REQUIRED

1. Verify the API after restart

First verify:

- API workflow is actually running.
- "/api/healthz" returns HTTP 200.
- No startup/database errors remain.
- Existing VAR HR frontend preview still works.

Do not assume success just because the workflow restart command completed.

2. Verify biometric devices

Test the actual implemented behavior:

- Create device.
- List devices.
- View device details.
- Edit device metadata where supported.
- Device status.
- Company isolation.
- Unauthorized access.
- Cross-company access.

Do not invent hardware communication.

If no real biometric vendor/protocol exists, synchronization must remain explicitly provider-neutral/unavailable.

3. Verify employee ↔ device mapping

Test:

- Map employee to device.
- View mappings.
- Change/remove mapping where supported.
- Company isolation.
- Unauthorized operations.
- Cross-company rejection.
- Duplicate mapping behavior.

4. Verify raw biometric events

Inspect the existing implementation and verify:

- Raw event creation/storage.
- Company ownership.
- Employee/device association.
- Duplicate/idempotency handling.
- Unauthorized access.
- Cross-company rejection.

Do not pretend that events came from physical hardware if there is no real device integration.

5. Finish GPS attendance

This is one of the remaining important items.

Verify and complete where necessary:

- GPS check-in.
- GPS check-out.
- Browser/device location capture.
- Latitude validation.
- Longitude validation.
- Timestamp handling.
- Accuracy handling if supported.
- Employee authorization.
- Company/location authorization.
- Inside-geofence detection.
- Outside-geofence rejection.
- Missing/denied location handling.
- Clear Arabic/English error states.

The previous agent specifically mentioned that browser GPS capture was the next implementation/verification step, so inspect the current code first and only add what is actually missing.

6. Verify company locations/geofences

Test:

- Create location.
- Edit location.
- Activate/deactivate location.
- Latitude.
- Longitude.
- Allowed radius.
- Company ownership.
- Cross-company protection.
- Invalid coordinates.
- Invalid radius.

7. Authorization

Verify server-side permissions for:

- Employee.
- Manager.
- Owner.

Especially:

- Employee cannot manage devices.
- Employee cannot manage company locations.
- Employee can only access their own GPS attendance.
- Manager is limited to their authorized scope.
- Owner can manage company devices and locations.
- Cross-company access is always rejected.

Do not rely only on hiding buttons in the frontend.

8. Frontend verification

Verify the actual live Preview for:

- Devices screen.
- Device mapping.
- Company locations/geofences.
- Attendance GPS behavior.
- Loading states.
- Empty states.
- Error states.
- Success states.
- Arabic translation.
- Arabic RTL.
- English.
- French.
- German.

Preserve the existing VAR HR design.

Do not redesign unrelated screens.

9. Validation

Run the appropriate project checks:

- Shared-library typecheck.
- API typecheck.
- Frontend typecheck.
- API build.
- Frontend build.
- API health.
- Live Preview.

If the workspace-wide build fails only because a workflow-owned Vite configuration requires managed environment variables, do not modify unrelated workflow configuration just to hide that issue. Record the exact limitation.

Important architecture rules

Keep:

OpenAPI → generated client → Express API → React frontend

Do not bypass the existing contract system.

Do not invent:

- Authentication provider.
- Biometric vendor.
- Fake hardware communication.
- Fake biometric synchronization.
- Production credentials.

Parts 1–3 are already complete. Do not modify them unless a confirmed Part 4 dependency requires it.

Database rule

If the imported development database is missing existing "var_hr_*" tables, use the existing non-destructive schema command:

"pnpm --filter @workspace/db run push"

Never:

- drop
- reset
- truncate
- delete existing data
- recreate the database unnecessarily

When Part 4 is actually finished

Only declare Part 4 complete after the implementation and runtime verification above pass.

Then create a NEW dated report inside:

"hand-off/"

Do not overwrite previous reports.

Update:

"hand-off/MASTER-HANDOFF.md"

The new report must clearly state:

- What was already completed before you started.
- What you completed.
- What was tested.
- What passed.
- What remains blocked, if anything.
- Whether Part 4 is:
  - Complete
  - Partially Complete
  - Blocked

Do not start Part 5.

Continue from the exact current state and finish Part 4 only.