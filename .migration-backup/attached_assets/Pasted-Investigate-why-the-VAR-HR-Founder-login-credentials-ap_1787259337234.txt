Investigate why the VAR HR Founder login credentials appear to become invalid whenever the project is moved to another environment/account.

The login currently reports that the username or password is incorrect.

I need you to determine whether the Founder account actually exists in the database used by the running API.

Do NOT change credentials or create a new Founder account yet.

Check the complete chain:

1. Identify the exact PostgreSQL database the running API is connected to.
2. Verify that the API is using the expected "DATABASE_URL".
3. Inspect the database schema and identify the table responsible for authentication/users.
4. Check whether the Founder user record actually exists in that database.
5. Verify the Founder account's:
   - username/email
   - role
   - account status
   - tenant/company association, if applicable
   - password hash presence
6. Inspect the authentication code to determine exactly how the password is verified.
7. Check whether the project has a Founder seed/bootstrap script.
8. Verify whether that seed actually runs automatically after deployment or database initialization.
9. Check migrations and seeds separately — do not assume that running migrations creates default users.
10. Compare the current environment with the previous working environment if the information is available.
11. Verify that the Preview/production API is connected to the same database that you are inspecting.
12. If there are multiple databases or environments, identify which one contains the original Founder account.

Also verify whether moving the project causes any of the following:

- a new empty database
- a different "DATABASE_URL"
- missing seed execution
- missing Founder record
- different password hashing configuration
- environment variables not being transferred
- authentication pointing to a different database than expected

Important

Do not simply reset the Founder password.

First prove whether the Founder record exists and whether the authentication system is checking the correct database.

At the end, report:

ROOT CAUSE:
Is the Founder account missing, is the API connected to the wrong database, is the password hash invalid/mismatched, or is something else causing the failure?

EVIDENCE:
Show the exact evidence from the database, environment configuration, authentication code, and server logs.

FIX:
Explain the minimum correct fix.

PERSISTENCE:
Make sure the solution will continue to work when the project is moved or deployed to another environment in the future.

Do not modify the code until the root cause is confirmed.