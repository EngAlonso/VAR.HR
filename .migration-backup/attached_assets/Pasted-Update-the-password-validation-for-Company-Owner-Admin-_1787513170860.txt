Update the password validation for Company Owner/Admin → Account Management → Create Account.

Required password rule

For newly created staff/employee accounts:

- Minimum password length: 6 characters
- No requirement for uppercase letters.
- No requirement for lowercase letters.
- No requirement for numbers.
- No requirement for special characters.
- No requirement for a specific character type.
- Any password with 6 or more characters should be accepted.

Examples that should be accepted:

- "123456"
- "abcdef"
- "أحمد123"
- "123456789"
- "@@@@@@"
- "abc 12"

Important

Trace the existing password validation across the Create Account flow and update the validation consistently wherever it is enforced for Company Owner-created staff accounts.

Do NOT weaken or change unrelated authentication/security behavior.

If the same password schema is shared with other account types, first verify whether changing it would unintentionally affect Founder/Platform Owner or other authentication flows. Only change the rule for the Company Owner-created employee/staff account flow unless the shared schema is intentionally meant to apply globally.

Error message

If the password is shorter than 6 characters:

Arabic:
"كلمة المرور يجب أن تحتوي على 6 أحرف على الأقل"

English:
"Password must contain at least 6 characters."

Use the existing i18n system. Do not expose raw Zod/API validation messages.

Verification

Test:

- 5-character password → rejected.
- 6-character password → accepted.
- Password containing only numbers → accepted.
- Password containing only letters → accepted.
- Password containing only symbols → accepted.
- Password containing Arabic characters → accepted.
- Password containing spaces → accepted, as long as the total length is at least 6.

Verify that account creation and login still work normally.

Run the existing typecheck/build/format checks afterward.

Do not change permissions, roles, authorization, tenant isolation, or unrelated account functionality.